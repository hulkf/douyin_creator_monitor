# Douyin Creator Monitor Instructions

本目录是一个独立自动化子项目，用于监控抖音达人主页、采集作品信息、转写视频音频文案，并把结果写入飞书等下游工具。

- 【运行与开发职责划分】本项目的流水线**实际运行不经 Codex / 任何 AI Agent**。运行由 `run_daily.bat` 驱动（双击即可），并已注册为 Windows 定时任务 `DouyinCreatorMonitor`（用仓库根 `install_task.bat` 一次性注册；双击运行，提示权限不足时右键"以管理员身份运行"）。当用户说"运行 / 跑一下"时，Agent 用 `schtasks /run /tn DouyinCreatorMonitor` 一键点燃，真正爬取/写飞书由任务计划程序在沙箱外执行，启动最快、运行期 0 token；**不要**自己 `python run_creator_pipeline.py`（会重新踩 Codex 沙箱审批/网络拦截）。Agent 只负责**改代码、排障、分析日志、改配置**。
- 改代码或排障前，先阅读本目录的 `README.md`、`MEMORY.md` 和相关 `docs/` 文档。`MEMORY.md` 里的审批避坑记录只在涉及 Codex 运行的历史排障中有参考价值，日常改代码无需重读，也不要为“运行”触发 CodeGraph 全量同步。
- 默认只处理本目录内的代码、配置、文档和运行产物，不要扫描或修改同级其他项目，除非用户明确要求。
- 对外部调度入口保持命令行可运行，便于后续接入 Windows 任务计划程序或其他调度器。
- 本地登录态、密钥、Cookie、飞书 ID、临时音视频、ASR 输出和测试日志放入 `local/`、`runtime/`、`logs/` 或 `output/`，不要提交真实敏感信息。
- 可提交内容只包括源代码、说明文档、非敏感配置模板和必要的示例结构。
- 这个项目当前重点内容方向是：抖音抖店推广系列内容、AI 自媒体与行业信息监控。

## CodeGraph

- 仓库根目录已初始化 `.codegraph/`。理解或定位本项目代码时，应先使用 CodeGraph，再使用 `rg` 或逐文件读取。
- 优先使用 `codegraph_explore` MCP 查询符号、源码和调用路径；MCP 暂不可用时，在仓库根目录运行 `codegraph explore "<问题或符号>"`。
- 修改代码后运行 `codegraph sync` 更新索引；索引数据库、日志和锁文件保留在本机，不得提交 Git。

## 抖音作品同步的数据保护规则

- 以 `抖音作品ID` 作为作品记录的唯一标识，不使用标题、链接或发布时间代替唯一键。
- 再次抓取到同一作品时，允许用本次获取到的更新信息覆盖飞书中该作品对应的已有字段。
- 覆盖更新只写入本次已确定获取到的字段；本次未获取到的值不得用空值清空历史数据。
- 飞书中已经存在、但本轮抖音抓取未找到的作品记录，必须原样保留；不得删除、不得清空、不得根据本轮作品列表做差集删除。
- 同步流程只能执行“新增缺失记录 + 覆盖更新已有记录”，禁止任何自动删除作品记录的逻辑。

## 作品入库必填数据

- 正式抓取并写入飞书作品表时，每条作品必须包含：`发布时间`、`点赞数`、`评论数`、`收藏数`、`分享数`。这些是核心数据，不得在正式同步中跳过。
- 上述数据必须优先通过 MediaCrawler 抓取抖音达人作品，并由 `scripts/collect_douyin_creator_with_mediacrawler.py` 规范化为本项目统一作品 JSON。底层仍应来自抖音作品列表数据：`create_time` 映射为发布时间，`statistics.digg_count/comment_count/collect_count/share_count` 或 MediaCrawler 等价字段分别映射为点赞、评论、收藏、分享数。
- Crawlio/Chrome 响应捕获只作为排障兜底或验证来源；默认采集链路应使用 MediaCrawler 项目框架。
- 主页卡片 DOM 只可用于发现作品或辅助校验，不得作为正式作品入库的唯一数据源。
- 正式同步必须在写入后做完整性校验；任一当前可见作品缺少上述核心字段时，不得将该轮任务报告为“已完成”，应继续补抓或明确报告异常。
- 对旧记录进行覆盖更新时，新抓到的完整核心数据可以更新对应字段；本轮未取到的核心值仍不得用空值覆盖历史值。
