# Feishu Schema Notes

## 达人基础信息表

真实表 ID 不写入可提交文档。请在本地私有配置中维护：

```text
douyin_creator_monitor/local/feishu-ids.md
```

关键字段：

- `达人昵称`
- `达人主页地址`
- `所属平台`
- `抖音UID`
- `SecUID`
- `账号ID`
- `账号简介`
- `粉丝数`
- `关注数`
- `获赞数`
- `作品数`
- `IP属地`
- `所在地区`
- `最近检查时间`
- `最近发稿时间`
- `主页采集状态`
- `账号状态`
- `主页原始数据`
- `作品表名称`
- `作品表ID`
- `作品表链接`

字段语义：

- `创建时间`: 飞书记录创建时间，不是抖音账号创建时间。
- `最近发稿时间`: 该达人最新非置顶作品的 `create_time` 转北京时间；测试时直接使用主页列表中最新作品时间。
- `所属平台`: 当前按主页 URL 域名推断，`douyin.com` 写为 `抖音`。

## 作品表规则

- 每个作品表只记录一个达人。
- 作品表名称使用达人昵称。
- 达人基础信息表通过 `作品表名称`、`作品表ID`、`作品表链接` 关联到对应作品表。

作品表关键字段：

- `抖音作品ID`
- `作品链接`
- `作品类型`
- `是否置顶`
- `作品标题`
- `原始文案`
- `话题标签`
- `封面图URL`
- `页面可见数字`
- `页面可见数字含义`
- `发布时间`
- `详情页状态`
- `视频时长秒`
- `点赞数`
- `评论数`
- `收藏数`
- `分享数`
- `通义听悟状态`
- `语音转写全文`
- `ima状态`
- `百度网盘状态`
- `本地知识库状态`
- `采集状态`
- `最近采集时间`
- `记录时间`
- `原始作品数据`

正式入库完整性要求：

- `发布时间`、`点赞数`、`评论数`、`收藏数`、`分享数` 是每条作品的必填核心数据。
- 正式同步不得因为主页卡片未展示这些数据而跳过；必须从作品列表接口的 `create_time` 和 `statistics` 中获取。
- 完成写入后必须检查当前可见作品的上述字段是否齐全；如有缺失，同步状态不得标记为已完成。

语音转写字段建议：

- `语音转写全文`: 火山引擎 ASR 识别后，再经过 `domain_glossary.json` 规则修正得到的最终正确文案。后续写入飞书、上传 IMA、本地知识库归档都使用这一份文案。

写回规则：

- 每个作品表只记录一个达人，写回时必须明确目标作品表 ID。
- 脚本默认用 `抖音作品ID` 搜索对应记录，然后更新同一条记录。
- 脚本默认只写 `语音转写全文` 一个字段，不写 ASR 原始 JSON、纠错报告、转写状态或完成时间。
- 不要用标题、发布时间或作品链接做唯一定位，避免误写到相似作品。
## IMA 归档字段

达人基础信息表已经加入 IMA 归档字段，用来把飞书达人记录和 IMA 知识库文件夹绑定起来。

字段：

- `IMA知识库名称`
- `IMA知识库ID`
- `IMA文件夹名称`
- `IMA文件夹ID`
- `IMA同步状态`

当前约定：

- `IMA同步状态 = 已映射`：该达人已在 IMA 中有对应文件夹，后续文案可直接上传到该文件夹。
- `IMA同步状态 = 待创建文件夹`：飞书里已有达人记录，但 IMA 里还没有对应文件夹。
- `IMA同步状态 = 待上传文案`：文件夹映射存在，但文案资料还没有完成上传。
- `IMA同步状态 = 异常`：IMA 查询、上传或映射校验失败，需要人工检查。

新增达人流程需要同时维护三类资产：

1. 在达人基础信息表中新建/更新达人记录。
2. 为达人创建或关联专属作品表，并回填 `作品表名称`、`作品表ID`、`作品表链接`。
3. 在 IMA「李建荣的知识库」中创建或定位同名文件夹，并回填 `IMA知识库名称`、`IMA知识库ID`、`IMA文件夹名称`、`IMA文件夹ID`、`IMA同步状态`。

当前流水线会先按达人名称精确查找 IMA 文件夹；不存在时调用 IMA `create_folder` 创建，取得 `folder_id` 后同时更新本地映射和达人基础信息表。该端点是已实测可用的未公开端点，如果 IMA 后续调整接口，流水线会把该平台阶段标记为失败并保留其他平台备份。

## 夸克归档字段

- `夸克文件夹名称`
- `夸克文件夹ID`
- `夸克文件夹路径`
- `夸克同步状态`

夸克 CLI 未返回文件夹 ID 时，只回写名称、路径和同步状态，不用空值清除飞书里已有 ID。

## Obsidian 归档字段

- `Obsidian文件夹名称`
- `Obsidian文件夹路径`
- `Obsidian同步状态`

Obsidian 是本地目录，没有云端文件夹 ID，因此只维护名称和绝对路径。

## 三个平台统一回写规则

- 默认使用 `SecUID` 在达人基础信息表中唯一定位达人；可在达人配置中用 `feishu_match_field`、`feishu_match_value` 覆盖。
- 每个平台先确保达人目录存在，再生成目录元数据，最后只更新该平台对应字段。
- 每轮会读取飞书现有值并与实际目录元数据比较；全部一致时跳过写入，只在新建目录、字段缺失或值不一致时更新差异字段。
- 不允许用空 ID、空路径覆盖飞书中的历史非空值。
- 匹配不到达人或匹配到多条达人记录时停止该平台的映射回写，不猜测目标记录。
